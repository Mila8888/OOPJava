package test;

public class Wall implements Barrier {

    private int height;

    public Wall(int height) {
        this.height = height;
    }

    @Override
    public boolean allBarrier(Participant participant) {
        return participant.run(height) >= height;

    }
}
