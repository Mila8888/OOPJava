package task1;

import static task1.Participants.OnDistance;

public class MainClass {
    public static void main(String[] args) {

        HumanClass human = new HumanClass("Вася", 40, 3);
        CatClass cat = new CatClass("Барсик", 100, 10);
        RobotClass robot = new RobotClass("Валли", 200, 20);

        Participants[] myParticapant = { human,  cat,  robot};

        for (Participants c : myParticapant)
            c.info();

        Barrier wall1 = new Wall(3);
        Barrier track1 = new RunningTrack(80);
        Barrier wall2 = new Wall(10);


        Barrier[] allbarrier = {wall1, track1, wall2};

        System.out.println();

        for (Participants c : myParticapant) {
            for (Barrier a : allbarrier) {
                    a.doIt(c);
            }
        }

    }
}
