package test;


public class Main {
    public static void main(String[] args) {

        Human human = new Human("Вася", 40, 3);
        Cat cat = new Cat("Барсик", 100, 10);
        Robot robot = new Robot("Валли", 200, 20);

        Participant[] myParticapant = {human, cat, robot};

        for (Participant c : myParticapant) {
            c.info();
        }

        Barrier wall1 = new Wall(3);
        Barrier track1 = new RunningTrack(50);
        Barrier wall2 = new Wall(3);

        Barrier[] barrier = {wall1, track1, wall2};

        System.out.println();


        outer: for (Participant c : myParticapant) {
            for (Barrier a : barrier) {
                boolean result = a.allBarrier(c);
                    if( a.allBarrier(c) == false )  {
                     break outer;
                    }else {
                        c.conclusion();
                    }
            }
        }

    }
}
