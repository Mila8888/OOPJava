package test;

public class RunningTrack implements Barrier {

    private int length;

    public RunningTrack(int length) {
        this.length = length;
    }

    @Override
    public boolean allBarrier(Participant participant) {
        return participant.run(length) >= length;

    }
}
